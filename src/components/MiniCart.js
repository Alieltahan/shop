import styled from 'styled-components';

const Container = styled.div`
  width: 32.5rem;
  height: 54rem;
  overflow: auto;
  background-color: #ccc;
`;
const MiniCart = () => {
  return <Container>My Bag</Container>;
};

export default MiniCart;
